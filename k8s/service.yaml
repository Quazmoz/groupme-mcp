apiVersion: v1
kind: Service
metadata:
  name: groupme-mcp
  namespace: default
  labels:
    app: groupme-mcp
spec:
  selector:
    app: groupme-mcp
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
      name: http
  type: ClusterIP
